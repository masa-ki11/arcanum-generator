# 引き継ぎメモ(2026-08-07)

v1.2.0 をリリースしたところ。**コードは全部コミット・プッシュ済み(main = `fcac332`)。**
実行ファイルもビルド済み。**未着手の相談ごとが1件、直せる見込みの穴が1件**残っている。

前回(2026-08-06)の「双の印」作業ぶんは完了してこのメモに置き換えた。

---

## 1. いまの状態

| | |
|---|---|
| ブランチ | `main`(origin/main と同期) |
| 最新コミット | `fcac332` Merge pull request #5 from masa-ki11/2人必須設定 |
| タグ | `v1.2.0` → `fcac332`(origin にプッシュ済み) |
| テスト | **151件 OK** / `gui.selftest()` OK |
| 実行ファイル | `dist/arcanum-generator-windows.zip`(12.4MB)。中身が新コードなのは埋め込みバイトコードを開いて確認済み |

### 作業ツリーに残っているもの

```
 D "kassen - コピー.json"     ← 追跡ファイルの削除が未コミット
```

`83eee10` で誤ってコミットされた連合データの控え。消したいなら
`git rm --cached "kassen - コピー.json"` してコミットする。
`kassen.json` は **skip-worktree** が立っているので、中身を書き換えても
`git status` に出ない(`git ls-files -v kassen.json` が `S` を返す)。これは意図的。

### `dist/` の掃除

`dist/arcanum-generator-windows/` は **8月3日ビルドの残骸**。今回のビルドで
更新されていないので、配るときに取り違えないよう消しておくとよい。
配布に使うのは `dist/arcanum-generator-windows.zip` か `dist/arcanum-generator/`。

---

## 2. v1.2.0 で入れたもの

### 「双(各戦2人)」の印(`392a013`)

奥義に付ける3つ目の印。どの戦にも確実に出せる担当(◎〇)が2人そろうまで足す。
**FILE_VERSION が 7 → 8 に上がった**(このバージョンで保存した `kassen.json` は
旧バージョンでは開けない。逆は読める)。詳細は README の該当節。

### 引き抜き交換 — 段階5.5 → 5.7 → 5.9(`83eee10`)

引き継ぎで全員の枠が埋まると、前回の顔ぶれがたまたま同じ戦に強い2人組だった
奥義に余分な担当が居座り、その戦を埋められない奥義が△頼み・×のまま残る。
そこから担当を引き抜いて回す段階。

**壊さないよう注意する点が4つある。** どれも実データで踏んだ落とし穴。

1. **段階5.5(落とす)より後に置く。**
   埋め直しには空き枠が要る。段階2〜4が穴埋めのために増やした担当が残ったままだと
   枠が塞がっていて代わりを入れられない。実データで、先に落とすかどうかで
   同じ盤面100回中の穴が **100回 → 0回**。11枠ぶんの差が出た。
   → `test_excess_is_dropped_before_swapping` が順序を固定している

2. **引き抜く前に、まず普通に足す。**
   段階5.5で空いた枠があるなら引き抜くまでもない。これを外すと後述の
   二重登録バグが復活する(候補に空き枠のある人が混ざるため)。

3. **余分でない担当も、代わりを入れられるなら引き抜く。**
   「中国 = tetsu(◎◎×) + OMEGA(△◎◎)」の形。tetsu は2戦目では余っているが
   1戦目のカバーを担っているので単純には抜けない。1戦目を確実に出られる
   空き枠の人と入れ替えれば抜ける。これが無いと実データの羊が直らなかった。
   → `test_non_redundant_member_is_pulled_when_a_stand_in_exists`

4. **×の戦には△でも引き抜く。**
   `_swap_in(..., sure=False)`。「△を引き抜いても確実にはならない」は正しいが、
   **誰も出られない戦を埋めるときは別**。実データで、1戦目に出られる唯一の△
   (ゆずぽこ)が確実な担当のいる奥義に居座って猿の1戦目が×になっていた。
   → `test_maybe_member_is_pulled_to_fill_an_empty_battle`
   → 逆側は `test_maybe_members_are_not_pulled_to_fix_a_maybe_dependent_battle`

#### 直した既存バグ

引き抜きを取り消すとき、埋め直しが本人を呼び戻していると **1人が同じ奥義を
二重に持つ**(`B: ['四人', '兵法', '四人', '水竜']`)。枠の勘定が合わなくなり
`ValueError: list.remove(x): x not in list` で落ちる。**2400回中26回(約1%)** 発生していた。
`_rollback` にガードを入れ、上の 2. と合わせて 0回。
→ `AllocationInvariantTest`(960通りの盤面で帳簿の整合性を検証)

### ＊印の修正 — `gui.carry_source()`(`83eee10`)

「引き継いで割り振る」を押すたびに `roster.carryover` が結果で上書きされるため、
**2回目からは「前回」が同じ日の1回目を指していた**。＊印と「担当が変わった人数」が
実際より少なく出る(実測: 2回目の＊が 3件 → 0件、「変わったのは0人」と嘘をつく)。

引き継ぎの起点をセッション内で据え置くようにした。**副産物として、押し直すたびに
別の組み合わせを引き直せるようになった**(以前は1回目の結果を引きずるだけだった)。

起点をリセットするのは「割り振る」でゼロから組んだとき・別ファイルを読んだとき・
新規作成・アプリ再起動。**参戦状況の編集では動かさない** — 編集が次の日ぶんなのか
打ち間違いの直しなのか区別できないので、差分を多めに出すほうへ倒している。
→ `CarryBaseTest`

---

## 3. 測定結果(いまの `kassen.json` を土台に)

### 引き抜き交換の効果(250日 / 参戦を毎日入れ替え)

| 入れ替え | △頼み | × | 2人未達 | 人数不足 | 引き継ぎ維持 |
|---|---|---|---|---|---|
| 0% | 0 → 0 | 0 → 0 | 0 → 0 | 0 → 0 | 100.0% → 100.0% |
| 15% | 485 → **294** | 144 → **95** | 38 → **19** | 6 → 6 | 87.4% → 86.9% |
| 30% | 1,092 → **692** | 281 → **178** | 74 → **54** | 34 → **29** | 78.4% → 78.2% |

△頼み約4割減、×3割減。引き換えに引き継ぎ維持率が 0.5〜1pt 下がる。

### 顔ぶれが前回とまったく同じだった割合(各400日)

| 参戦の変化 | 必須奥義 | 瞬時(何度も) | 担当が変わらない人 | 担当の維持率 |
|---|---|---|---|---|
| **0%** | **100.0%** | **100.0%** | **100.0%** | **100.0%** |
| 5% | 89.5% | 54.3% | 82.2% | 94.7% |
| 15% | 74.8% | 20.1% | 62.0% | 87.4% |
| 30% | 53.7% | 3.9% | 42.0% | 77.3% |

**参戦が変わっていなければ1件も動かない**(0%の行)。押し直しても落ち着く。
連合の実測変化量は **60枡中17枡 = 28%**(20人中11人)だったので、だいたい30%の行。

---

## 4. 残っているもの

### (A) 相談ごと — 「猛虎や花鳥だけの入れ替えで変更者が出るのが嫌」【未着手】

きっかけになった実出力:

```
猛虎(6人 ...): ゆずぽこ、jt、ピコ、BLUE＊、ふろん太＊、さぁし＊
天下(6人 ...): ふろん太、さぁし＊、shinsaku＊、kino＊、B＊、BLUE＊
                 ↓
－－－ さぁし: 栄枯、天下、花鳥、猛虎 ★   ← 瞬時4件しか持てない人なのに★
```

**印は2種類ある**(どちらを直すかで結果が変わる):

| 印 | 出どころ | 描画 |
|---|---|---|
| `＊` 奥義の行の名前の後ろ | `CarryoverReport.is_new()` | `storage.py:173` |
| `★` メンバー行の末尾＋人数 | `changed_members()` / `change_note()` | `storage.py:227,240` |

どちらも `allocator.record_changes()` が `load` と引き継ぎ元を突き合わせて作る。
**「瞬時(何度も)」を除外するだけなら小さい変更で済む。**

**測定と実感が食い違っている。** 1600日ぶん(瞬時の件数を4/5/6件に振って)回したが、
**瞬時だけで★が付くケースは0件**だった。原因はおそらく、段階6が前回の
「瞬時(何度も)」を先に戻すので、必須が動かない限り余り埋めも動かないため。
上の実例は**奥義リスト自体をいじった直後**(四人・激流・咲花を追加した回)に
余り埋めが総入れ替えになったものと思われる。**再現条件の特定が先。**

未確定の論点(本人に聞く途中で中断):
1. 除外するのは「瞬時(何度も)」だけか、**もちまる・心臓・宴楽・四人・激流を含む「瞬時」系全部**か
2. `＊` と `★` の両方を直すか、`★` だけか
3. 消すのではなく「変わったのは必須3人(瞬時込み8人)」と**併記**する案もある

### (B) 前衛の穴は引き抜きの守備範囲外【直せる見込みあり】

`_swap_in` は確実な担当(◎〇)の穴と×しか見ておらず、**前衛のカバーは対象外**。
引き継ぎで前衛になれる人が他の奥義に固定されると、前衛向け奥義に前衛が付かない。

実データで確認: 2戦目に前衛として出られるのが華・うらん・ゆうくんの3人だけの盤面で、
**引き継ぎだと前衛穴2件、ゼロから組むと0件**。つまり避けられる穴。
`_swap_in` の候補条件に `is_vanguard(battle) and joins(battle)` を足せば拾えるはず(未検証)。

### (C) 1戦目の△は仕様どおり(直す対象ではない)

いまの `kassen.json` で `沖・月・猿・狼` の1戦目が△頼みで残るのは**数学的に不可避**。

- 1戦目に確実に出られるのは **7人 = 28枠**
- 要るのは **32枠**(必須28件 + `双`4件の2人目)

`双` を1件外すごとに1戦目の穴が1件減る(4件→0件まで実測で1対1に対応)。
つまりこの4件は **`双` の印4件のコストそのもの**。

---

## 5. 作業の再開手順

```bash
git switch main && git pull
.venv/Scripts/python.exe -m unittest discover -s tests -q   # 151件 OK
.venv/Scripts/python.exe -c "from arcanum_generator.gui import selftest; raise SystemExit(selftest())"
.venv/Scripts/python.exe main.py                            # 起動
python build.py                                             # 配布ファイル作り直し
```

### 検証のときにハマった罠

- **アプリを起動したままソースを直しても反映されない。** Python は起動時に
  読み込んだモジュールを使い続ける。このセッションで2回これに引っかかった。
  必ずアプリを閉じて `run.bat` から開き直すこと
- **`kassen.json` は頻繁に書き換わる。** 測定結果は「いつの `kassen.json` か」で
  変わる。比較するときは同じ時点のデータで揃えること
- **参戦しない人(`－－－`)を乱数で書き換えると実態を外す。** さぁし・BLUE のように
  ずっと不参加の人は固定して測らないと、余り埋めの挙動が再現できない
- 総当たり/シミュレーションのスクリプトは使い捨てで残していない。必要なら
  README の測定値のそばに条件を書いてあるので、そこから組み直す
